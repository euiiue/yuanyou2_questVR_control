# Python runtime dependencies for the QuestArmTeleop ROS 2 nodes.
#
# ROS 2 packages such as rclpy, geometry_msgs, sensor_msgs, tf2_ros, rviz2,
# robot_state_publisher and joint_state_publisher are installed through apt
# on the Jetson Foxy runtime and are not pip packages.
#
# Pinocchio is installed from conda-forge in the Jetson setup flow:
#   conda install -c conda-forge pinocchio==3.2.0

numpy>=1.20
scipy>=1.8
PyYAML>=5.3
pure-python-adb>=0.3.0
python-can>=4.0
typing-extensions>=4.0
casadi>=3.5
meshcat>=0.3
